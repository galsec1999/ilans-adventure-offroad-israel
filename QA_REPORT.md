# דוח בקרת איכות — גרסת מסמך 2.4.0

גרסת מוצר שנבדקה: **2.5.1**
גרסת המסמך הראשי: **2.3.3**
תאריך: **2026-08-09**

## תוצאה מקומית

כל שבעת שערי הקוד והנתונים עברו ללא כשל, וכן שער חדש בן 16 בדיקות למצבי התצוגה. בדיקת התחביר של JavaScript ו־`git diff --check` עברו.

## כיסוי אמינות

- 339/339 כרטיסים קיבלו תוצאת התאמה.
- 295/295 מזהי Track נבדקו מחדש; 290 שכבות מפה נפתחו.
- 285/290 שכבות זמינות תואמות לנקודות המקור; 5/290 מכילות סתירה פנימית מתועדת; 5/295 מקורות אינם זמינים.
- 551,740 נקודות תוואי ו־20,614 ישויות ממופות נבדקו.
- 272 כרטיסים כוללים שמות חיפוש גאוגרפיים; לכל שם מוצג מרחק מן הקו.
- לכל 339 הכרטיסים יש מאפיין חיפוש יחיד; אין כפילות `data-search`.
- 276 כרטיסי Off‑Road מציגים לוח אימות תוואי; חמש הסתירות מוצגות במפורש.

## שימור מוצר

עברו: 339 כרטיסים, 100 מקורות מחקר נפרדים, סינון ומיון, מצב בהיר, מצב חשוך, „לפי המכשיר”, שמירת הבחירה, מונה כניסות, הזמנה עשירה וקצרה, כרזה ו־QR, בחירת מפה בכרטיס רב־מפה, AI מקומי ללא מפתח, הקראה, ייצוא HTML מלא, noindex, robots.txt, היעדר sitemap, manifest יחסי, סמלי PWA ו־Service Worker בנתיב משנה.

## מגבלה

בדיקת GUI מקומית למסמך 2.3.3 עברה במובייל ובמחשב: שני המצבים, שמירה לאחר רענון, 339 כרטיסים, אפס גלישה אופקית ואפס שגיאות מסוף. גישה חזותית אוטומטית לדומיין GitHub Pages נחסמה בהגדרת הרשאה שמורה; לא בוצע מעקף ואין טענה שסמל ההתקנה המקורי של Chrome נבדק אוטומטית.

## פרסום

GitHub Pages פרס בהצלחה את commit `148fa0782af15ae77243d17301df0922a28471d1` בריצה `31310269114`. באתר החי אומתו מוצר 2.5.1, מסמך 2.3.3, שני כפתורי התצוגה, 339 כרטיסים, noindex, manifest יחסי במצב standalone, מטמון `2.5.1-doc-2.3.3`, קוד התקנה ושלושה סמלי PWA. `robots.txt` תואם במדויק ו־`sitemap.xml` מחזיר 404.
