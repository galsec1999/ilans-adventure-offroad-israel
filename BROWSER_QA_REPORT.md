# דוח בדיקת דפדפן וקבלת מוצר — גרסת מסמך 1.2.1

גרסת מוצר שנבדקה: **2.5.0**
גרסת המסמך הראשי שנבדקה: **2.3.2**
תאריך: **2026-08-09**

המסמך הסופי הוגש מקומית ב־`127.0.0.1:8766`, אך הדפדפן המובנה דחה גישה בגלל הרשאת משתמש שמורה. בהתאם למדיניות הבטיחות לא נעשה ניסיון לעקוף את החסימה דרך CDP, דפדפן חלופי או כלי עקיף.

בדיקת GUI מוקדמת יותר של מוצר 2.5.0 לפני שילוב הגאומטריה אישרה 339 כרטיסים, 100 כרטיסי מחקר, אפס גלישה אופקית, מצב כהה, הזמנה, AI, ייצוא ו־Service Worker. מאחר שהמסמך הראשי עלה מאז ל־2.3.2, תוצאה זו אינה מוצגת כאימות GUI מלא של הקובץ הסופי.

במסמך 2.3.2 אומתו סטטית: 339 כרטיסים, 276 לוחות תוואי, 272 כרטיסים עם אינדקס גאוגרפי, מאפיין חיפוש יחיד לכל כרטיס, בורר הקלטה בכרטיס רב־מפה, נתיבי manifest/SW יחסיים ומטמון `2.5.0-doc-2.3.2`.

בדיקת GUI חיה ובדיקת סמל ההתקנה המקורי של Chrome נשארות שער קבלה ידני; הן אינן חוסמות את תקינות קבצי ה־PWA אך אינן נטענות כבדיקות שבוצעו.

## אימות חי שאינו GUI

GitHub Pages השלים build ו־deploy. מן הכתובת החיה התקבלו HTTP 200 לדף, manifest, Service Worker, JavaScript, שני דוחות האמינות ושלושת סמלי ה־PWA. אומתו 339 כרטיסים, 276 לוחות תוואי, חמש אזהרות מקור, מוצר 2.5.0, מסמך 2.3.2, noindex, robots מלא ו־sitemap=404. ה־manifest מציג `start_url` ו־`scope` יחסיים ומצב `standalone`.
