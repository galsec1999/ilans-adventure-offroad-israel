# דוח בדיקת דפדפן וקבלת מוצר — גרסת מסמך 1.3.0

גרסת מוצר שנבדקה: **2.5.1**
גרסת המסמך הראשי שנבדקה: **2.3.3**
תאריך: **2026-08-09**

המסמך הסופי הוגש מקומית ב־`127.0.0.1:8767` ונבדק ברוחב 390 פיקסלים ובתצוגת מחשב. שני הכפתורים היו גלויים; מצב חשוך שינה את רקע הגוף ל־`rgb(21, 28, 24)` ואת הטקסט ל־`rgb(242, 234, 220)`, ומצב בהיר חזר לפלטה הבהירה. `aria-pressed` והודעת המצב התעדכנו, והבחירה החשוכה נשמרה לאחר רענון.

בדף המקומי הסופי נספרו 339 כרטיסים, אפס גלישה אופקית ואפס שגיאות מסוף. לאחר החזרת viewport לברירת המחדל נשמרו מצב בהיר ואפס גלישה אופקית.

במסמך 2.3.3 אומתו סטטית: החלה מוקדמת לפני CSS, שני כפתורי תצוגה, בורר בהיר/חשוך/מערכת, שמירה וסנכרון, 339 כרטיסים, נתיבי manifest/SW יחסיים ומטמון `2.5.1-doc-2.3.3`.

בדיקת GUI ישירה בדומיין החי נחסמה בידי הרשאת משתמש שמורה בדפדפן המובנה ולא נעשה מעקף. בדיקת סמל ההתקנה המקורי של Chrome נשארת בדיקת קבלה ידנית; תקינות דרישות ההתקנה אומתה דרך manifest, Service Worker, HTTPS, scope יחסי ושלושת הסמלים.

## אימות חי שאינו GUI

GitHub Pages השלים build ו־deploy בריצה `31310269114`. מן הכתובת החיה התקבלו HTTP 200 לדף, manifest, Service Worker, CSS, JavaScript, robots.txt ושלושת סמלי ה־PWA. אומתו 339 כרטיסים, שני כפתורי התצוגה, מוצר 2.5.1, מסמך 2.3.3, noindex, robots מלא ו־sitemap=404. ה־manifest מציג גרסה 2.5.1, מסמך 2.3.3, `start_url` ו־`scope` יחסיים, מצב `standalone` ושלושה סמלים.
