# דוח בדיקות יומן השטח המקומי — גרסת מסמך 1.2.0

גרסת מוצר שנבדקה: **2.6.0**
גרסת מסמך האתר: **2.4.0**
תאריך: **2026-08-09**

## תוצאה

שער הבדיקות האוטומטי עבר. בדיקת דפדפן מקומית אימתה כותרת 2.6.0/2.4.0, כפתור „היומן שלי”, 339 כפתורי יומן למסלולים, פתיחת חלון היומן ושמירת דיווח שמופיע במונה וברשימה. האתר מוגש ב־`http://127.0.0.1:8767/`.

## בדיקות שעברו

- 339 כרטיסי מסלול נשמרו, עם 339 אזורי פעולות ו־339 כפתורי ייצוא HTML.
- לכל כרטיס מוזרק כפתור הערות וביקורות ומונה מקומי.
- שמירת דיווח, הצגתו, עריכתו ומחיקתו ב־IndexedDB עברו בבדיקת DOM מבודדת.
- בדפדפן המקומי נשמר דיווח בפועל והמונה השתנה ל־1; תוכן ההערה, הביקורת, הדירוג ושם הבודק הוצגו ברשימה.
- גיבוי JSON, מחיקה וייבוא מחדש עברו; הייבוא משחזר דיווח בלי לשכפל אותו.
- ביקורת מהאינטרנט שומרת סוג מקור וקישור; קישורי `javascript:` נדחים בייבוא.
- גיבוי מגדיר `sharing.syncState` כ־`local-only` ואינו מבצע תקשורת לשרת.
- ייצוא HTML של מסלול כולל את ההערה, הביקורת וקישור הסרטון המקומיים.
- Manifest תקין ומציג גרסת מוצר 2.6.0 ומסמך 2.4.0.
- Service Worker עבר לגרסת מטמון `2.6.0-doc-2.4.0` וכולל את `field-notes.js` במעטפת הלא־מקוונת.
- `noindex`, ‏`robots.txt`, היעדר sitemap ונתיבי המשנה של GitHub Pages נשמרו.
- בדיקות מטא־דאטה Off‑Road הקיימות עברו: 339 כרטיסים, 276 עם Track, ‏295 מזהי Track, ‏290 מאומתים וחמישה לא זמינים.
- כל שבע חבילות שימור התכונות, המטא־דאטה, המפות, הגאומטריה והמצב הבהיר/כהה עברו.
- 410 מקורות תמונה מקומיים נמצאו ונקראו; תמונות 017 ו־018 נטענו בדפדפן בגודל 1200×675.
- בדיקות תחביר JavaScript עברו.
- בדיקת HTTP מקומית החזירה 200 עבור `index.html`, ‏`field-notes.js`, ‏`manifest.webmanifest` ו־`sw.js`.

## מגבלות שנבדקו בקוד

- עד שש תמונות לדיווח ועד 10MB לתמונת מקור.
- התמונות מוקטנות עד 1600 פיקסלים ומקודדות מחדש ל־WebP או JPEG להסרת EXIF.
- בייבוא מתקבלות רק תמונות PNG, ‏JPEG או WebP, עם מגבלות גודל וכמות.
- קישורי מקור וסרטונים מוגבלים ל־HTTP/HTTPS.
- טקסטים, מספרי רשומות וגודל קובץ הייבוא מוגבלים.
- מחיקת נתוני האתר עדיין יכולה למחוק את העותק המקומי; הממשק מזהיר ומציע גיבוי.

## בדיקות קבלה אופציונליות שנותרו

1. להוסיף דיווח עם תמונה אמיתית מן הטלפון ולבדוק את התצוגה והגיבוי במכשיר פיזי.
2. לבדוק את סמל ההתקנה המקורי של Chrome לאחר הפרסום.
3. לייצא גיבוי מן הכתובת המקומית ולייבא אותו באתר החי אם נשמרו נתוני אמת ב־localhost.

הגרסה לא פורסמה ולא נדחפה ל־GitHub.
